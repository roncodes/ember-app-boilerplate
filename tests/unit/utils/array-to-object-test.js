import arrayToObject from '@boilerplate/dashboard/utils/array-to-object';
import { module, test } from 'qunit';

module('Unit | Utility | array-to-object', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = arrayToObject();
    assert.ok(result);
  });
});
