import isArrayOfModels from '@boilerplate/dashboard/utils/is-array-of-models';
import { module, test } from 'qunit';

module('Unit | Utility | is-array-of-models', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = isArrayOfModels();
    assert.ok(result);
  });
});
