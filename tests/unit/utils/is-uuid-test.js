import isUuid from '@boilerplate/dashboard/utils/is-uuid';
import { module, test } from 'qunit';

module('Unit | Utility | is-uuid', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = isUuid();
    assert.ok(result);
  });
});
