import first from '@boilerplate/dashboard/utils/first';
import { module, test } from 'qunit';

module('Unit | Utility | first', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = first();
    assert.ok(result);
  });
});
